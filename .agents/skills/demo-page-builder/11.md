<agent-message from="general-purpose">
    已阅读全部三个文件且未修改。确认 10 项具体缺陷，建议按严重度报告：

    1. `/root/prd-demo-react/.agents/skills/demo-page-builder/SKILL.md:24-26`：更新单个 skill 时执行 `cp -r
    .../.agents/skills/. .agents/skills/`，会覆盖远端仓库内的全部 skill，而非仅
    `demo-page-builder`。失败场景：项目有本地定制或同名 skill，任一 SKILL.md 检出差异后它们都被静默覆盖。修正：只更新该
    skill 自身；覆盖前展示变更并征得确认，保留本地文件。

    2. `/root/prd-demo-react/.agents/skills/demo-page-builder/SKILL.md:14-30`：`curl ... | diff - ...`
    没有分别检查下载与 diff 状态；默认管道状态取 `diff`，curl 超时/403 时 diff 会把空输入判为“有差异”(退出 1)，从而进入
    clone，而不是第 30 行规定的“检查失败后用本地版”。固定 `/tmp/prd-demo-skills` 也会在残留目录存在时令 clone
    直接失败。修正：先下载到 `mktemp`，单独验证 curl；再区分 diff 退出码 0/1/>1，并用 trap 清理临时目录。

    3. `/root/prd-demo-react/.agents/skills/demo-page-builder/SKILL.md:12-29,59`：每次触发都以未固定 commit/校验和的
    `master` 覆盖本地内容，与“用户修改本 skill 白名单即可解锁”自相矛盾。失败场景：用户按第 59 行把 X
    加入白名单，下一次触发立即被远端版本回滚；远端内容变化还会在无确认下改变本次 agent
    行为。修正：采用固定版本/发布包和完整性校验；本地修改存在时不得自动覆盖，升级必须显式确认或三方合并。

    4. `/root/prd-demo-react/.agents/skills/demo-page-builder/references/package.json:12,17`，`/root/prd-demo-react/.ag
    ents/skills/demo-page-builder/references/dev-server.md:89`，`/root/prd-demo-react/.agents/skills/demo-page-builder/
    SKILL.md:85`：基准依赖本身是无效 peer 组合：`@ant-design/x@2.1.0` 声明 `antd ^6.0.1`，清单却是 `antd
    ^5.25.0`，流程靠 `--legacy-peer-deps` 强行安装并把故障延后到运行时。失败场景：实现 AI 页面后 X 使用 antd 6
    API/上下文而崩溃；此时才让用户二选一会返工整个实现。修正：在基准清单中预先选定受支持组合（antd 5 + X
    1.x，或整体迁移 antd 6），不要把 legacy peer 绕过当正常安装路径。

    5. `/root/prd-demo-react/.agents/skills/demo-page-builder/references/package.json:80`，`/root/prd-demo-react/.agent
    s/skills/demo-page-builder/references/dev-server.md:13-19,55`：环境检查只确认“是真 Node”，不校验版本，同时
    `engines` 声称 Node >=12；但直接依赖 TypeScript 5.6.3 要求 Node >=14.17，npm 11 又要求 Node ^20.17 或
    >=22.9。失败场景：机器有真实 Node 12，流程直接复用并在安装/构建阶段才失败。修正：声明实际测试过的 Node
    范围，并在复用现有 runtime 前做 semver 检查；不要以失真的 `>=12` 指导安装。

    6. `/root/prd-demo-react/.agents/skills/demo-page-builder/references/dev-server.md:39-55`：标称 POSIX 同时支持
    Linux/macOS 和多架构，但可执行示例固定下载 `linux-x64.tar.xz` 并固定该目录进 PATH。失败场景：macOS 或 Linux ARM
    按主步骤执行，下载错误平台二进制，node 无法运行；第 54 行的说明不会修正前面必执行命令。修正：在命令中先映射 OS/arch
    并构造正确文件名/解压格式，或分别提供可直接执行的平台脚本。

    7. `/root/prd-demo-react/.agents/skills/demo-page-builder/SKILL.md:3,8,83`，`/root/prd-demo-react/.agents/skills/de
    mo-page-builder/references/dev-server.md:5,83,95-99`：skill 描述将范围扩大到“一切前端页面”，实际却硬编码
    `/root/prd-demo-react`、Umi `src/pages`、`max dev` 及固定局域网 IP。失败场景：在
    Vite/Next/其他目录触发后会写错目录、复制 Umi 包清单或运行不存在的 `max`。修正：frontmatter 明确限定该仓库/Umi
    Max，或先探测框架和项目根再选择适配流程；网络地址从实际启动输出获取，不能写死。


    8. `/root/prd-demo-react/.agents/skills/demo-page-builder/references/dev-server.md:83` 与
    `/root/prd-demo-react/.agents/skills/demo-page-builder/references/package.json:14-16,36-42`，对照
    `/root/prd-demo-react/.agents/skills/demo-page-builder/SKILL.md:32-42,83`：包文件丢失时被要求整份复制“基准”，会引入
    TinyMCE React、`@uiw/react-md-editor`、`react-sketch-canvas` 等白名单外 UI/React 组件及许多与当前页面无关依赖，直
    接违反“禁止引入白名单外依赖”。修正：基准只保留框架及白名单必需项；图表/编辑器等按需求、经规则明确授权后单独安装。

    9. `/root/prd-demo-react/.agents/skills/demo-page-builder/SKILL.md:96-103` 与
    `/root/prd-demo-react/.agents/skills/demo-page-builder/references/dev-server.md:57`：前者命令 agent 无条件用
    apt/yum/brew/winget 安装 git，并在无仓库时直接 `git init`；后者则规定任何系统级安装须先获用户同意。失败场景：缺 git
    时未经确认修改系统或因无管理员权限阻塞；非仓库目录还会被擅自纳入版本控制。并且第 103 行 `git add -A`
    会把用户原有未提交改动一起提交。修正：统一为安装/git init/commit 前显式确认；先检查 dirty 状态，只暂存 agent
    本次修改的明确文件列表，绝不使用全局 `add -A` 吞入无关改动。

    10. `/root/prd-demo-react/.agents/skills/demo-page-builder/references/dev-server.md:95-107`，`/root/prd-demo-react/
    .agents/skills/demo-page-builder/SKILL.md:87-91`：`npm run dev`
    示例没有实际后台化方式，注释“后台运行，disable_timeout”不是命令，重启又依赖 Claude Code 专有
    `TaskStop`；同时要求未获用户浏览器确认绝不能完成且无超时/跳过状态。失败场景：不具备 TaskStop 的 agent 被前台 dev
    server 永久占住；API/无人值守会话即使自动验证成功也永远无法结束。修正：明确限定运行宿主并给出可执行的后台任务/PID
    生命周期流程；把“技术验收完成”与“用户主观确认”分开，提供一次确认请求和可恢复的待反馈状态，而不是无限阻塞完成。
    </agent-message>